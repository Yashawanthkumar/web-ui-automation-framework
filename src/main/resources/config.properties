# config.properties

browser=chrome
baseUrl=https://example.com                  
implicitWait=10
explicitWait=15
headless=false
environment=local                           
retryFailedTests=true
logLevel=INFO
environment=local   # or docker
browser=chrome
gridUrl=http://localhost:4444/wd/hub

# Base URL configuration
baseURL_local=https://your-local-app.com
baseURL_docker=http://host.docker.internal:8080
